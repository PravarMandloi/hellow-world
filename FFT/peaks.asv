function [ y ] = peaks( x,f,fs )
%UNTITLED5 Summary of this function goes here
%   Detailed explanation goes here
a=length(x);
fk=(a/fs)
p=x(f-2:f+2);
[a,b]=max(p);
if b == 3 
    y=1;
    
else 
    y=0';
end

end

